name: Deploy Doxygen to Pages

on:
  push:
    branches: ["main"]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install Doxygen
        run: |
          sudo apt-get update
          sudo apt-get install -y doxygen graphviz

      - name: Build Android Docs
        run: |
          cd docs
          doxygen Doxyfile.android

      - name: Build Linux Docs
        run: |
          cd docs
          doxygen Doxyfile.linux

      - name: Create Landing Page
        run: |
          cat <<EOF > docs/index.html
          <!DOCTYPE html>
          <html>
          <head><title>Project Documentation</title></head>
          <body style="font-family: sans-serif; padding: 40px;">
            <h1>API Reference</h1>
            <hr>
            <ul>
              <li><a href="android/index.html">Android Documentation</a></li>
              <li><a href="linux/index.html">Linux Documentation</a></li>
            </ul>
            <p><small>Last updated: $(date)</small></p>
          </body>
          </html>
          EOF

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
